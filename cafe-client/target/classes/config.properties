# =====================================================================
# Kiosk Client config (Computer C). Place a copy next to the JAR.
# =====================================================================

# Unique identifier announced to the server via IDENTITY:<id>
station.id=101

# Central server (Computer B)
server.host=192.168.61.30
server.port=6969

# Auto-reconnect attempt interval (seconds) if the LAN drops.
reconnect.interval.seconds=5
