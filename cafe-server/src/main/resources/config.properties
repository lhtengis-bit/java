# =====================================================================
# Cafe Server config (Computer B). DO NOT hardcode credentials in code.
# Override at runtime by passing -Dcafe.config=/path/to/file.properties
# =====================================================================

# --- MySQL (Computer A) ---
db.url=jdbc:mysql://127.0.0.1:3306/pc_cafe?
useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=88820854

# --- TCP server (this machine) ---
server.bind.host=192.168.62.58
server.port=6969
server.thread.pool.size=16
server.heartbeat.interval.seconds=15

# --- Cashier UI ---
ui.refresh.interval.seconds=2
ui.default.locale=en

# --- JavaMail (end-of-shift report) ---
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.smtp.auth=true
mail.smtp.starttls.enable=true
mail.from=cafe.cashier@example.com
mail.from.password=YOUR_APP_PASSWORD
mail.to=manager@example.com
